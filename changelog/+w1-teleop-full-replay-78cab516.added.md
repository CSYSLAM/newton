Add full-state W1 teleoperation JSONL replay through the bag packing demo's `--replay` option. Record joint and rigid-body velocities alongside robot poses and bag deformation, restore frames without physics, and support seeking and interrupted recording prefixes. Reject a changed recording output path when resuming an existing teleoperation process instead of silently continuing to write the old file.
