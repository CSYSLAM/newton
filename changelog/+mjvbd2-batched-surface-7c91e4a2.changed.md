Accelerate the MJVBDV2 `surface-fast` preset with a guarded two-batch Jacobi-Chebyshev surface schedule; set `particle_enable_batched_jacobi=False` in `vbd_options` to retain ordinary colored sweeps.
